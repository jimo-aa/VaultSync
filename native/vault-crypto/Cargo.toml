# 最底层 crate：随机数、Hash、Argon2id、HKDF、AES-256-GCM、Zeroize。
# P1-1 落地：本 crate 不得依赖任何 OS / 平台能力，保持纯密码学原语。
[package]
name = "vault-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
aes-gcm = "0.10"
argon2 = "0.5"
hmac = "0.12"
sha2 = "0.10"
rand = "0.8"
zeroize = "1.8"
