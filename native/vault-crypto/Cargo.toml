# 最底层 crate：随机数、Hash、Argon2id、HKDF、AES-256-GCM、Zeroize（P1 落地）。
# P0 骨架阶段不引入外部依赖，仅固化边界与测试基线。
[package]
name = "vault-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
